services:
  web:
    image: ${APP_IMAGE}
    # build when image doesn't exist
    build:
      context: .
      dockerfile: Dockerfile

    depends_on:
      - "redis"
    ports:
      - "5000:5000"
    environment:
      - REDIS_HOST=${REDIS_HOST}

  redis:
    image: "redis:alpine"
